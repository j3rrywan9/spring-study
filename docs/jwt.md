# JWT

JWT is a string that has three parts, each separated by a dot.
Each section is Base64 encoded.
The first section is the header, which gives a clue about the algorithm used to sign the JWT.
The second section is the body, and the final section is the signature.
